<!-- markdownlint-disable-file MD041 MD001 -->
!!! note "Environment required"
    Vertex batches require `VERTEX_PROJECT_ID`, `VERTEX_GCS_PREFIX`, and either
    `GEMINI_API_KEY` or `GOOGLE_API_KEY` to be loaded in your environment before
    running batches.
