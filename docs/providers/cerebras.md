# Cerebras

`batchling` is compatible with Cerebras through any [supported framework](../frameworks.md){ data-preview }

The following endpoints are made batch-compatible by Cerebras:

- `/v1/chat/completions`

## Example Usage

Here's an example showing how to use `batchling` with Cerebras:

```python
--8<-- "examples/providers/cerebras_example.py"
```
