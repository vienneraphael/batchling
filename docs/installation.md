# Installation

batchling is available on PyPI as `batchling`, install using either `pip` or `uv`:

## Package manager

=== "uv"

    ```bash
    uv add batchling
    ```

=== "pip"

    ```bash
    pip install batchling
    ```
